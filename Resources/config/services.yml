parameters:
    db:   '%mongodb.db%'
    host: '%mongodb.host%'

services:
    mongodb.manager:
        class: Budby\MongoDbBundle\Manager\MongoDbManager
        calls:
            - [ setConfigData, [ '%db%', '%host%' ] ]
